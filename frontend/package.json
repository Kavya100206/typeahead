{
  "name": "typeahead-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Plain TypeScript typeahead UI, no bundler",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
